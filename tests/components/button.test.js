import CustomButton from "components/button";
import { render, screen } from "@testing-library/react";

describe("Button Component", () => {
  test("should get children", () => {
    render(<CustomButton type="red">This Button</CustomButton>);
    expect();
  });
});
